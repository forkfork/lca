# Invoice totals

Implement `total(subtotal, tax_percent)` for nonnegative integer cents and a nonnegative integer tax percentage. Add tax to the subtotal, rounding fractional tax cents down. Preserve the function signature.

Verify with `python3 -B -m unittest discover -s tests -v`.

Do not modify or add tests, documentation, or tooling.
