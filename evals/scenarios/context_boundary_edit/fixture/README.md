# Pipeline continuation task

The exact requested values and scope were established in the active conversation.
Continue from the latest correction rather than any superseded earlier proposal.

The configuration lives in `pipelines/rules.py`. Verify with:

```bash
python3 -m unittest discover -s tests -v
```
